<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:v2rayN.Converters"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <system:Double x:Key="MenuItemHeight">32</system:Double>
    <system:Double x:Key="StdFontSize">12</system:Double>
    <system:Double x:Key="StdFontSize1">13</system:Double>
    <system:Double x:Key="StdFontSize-1">11</system:Double>

    <conv:InverseBooleanConverter x:Key="InverseBooleanConverter" />

    <Thickness x:Key="Margin4" Bottom="4" Left="4" Right="4" Top="4" />
    <Thickness x:Key="Margin8" Bottom="8" Left="8" Right="8" Top="8" />
    <Thickness x:Key="OutlinedTextBoxDefaultPadding" Bottom="8" Left="8" Right="8" Top="8" />
    <Thickness x:Key="MarginLeftRight4" Bottom="0" Left="4" Right="4" Top="0" />
    <Thickness x:Key="MarginLeftRight8" Bottom="0" Left="8" Right="8" Top="0" />
    <Thickness x:Key="MarginLeft8" Bottom="0" Left="8" Right="0" Top="0" />
    <Thickness x:Key="MarginRight8" Bottom="0" Left="0" Right="8" Top="0" />

    <SolidColorBrush x:Key="NetCatWindowBackgroundBrush" Color="#0B1220" />
    <SolidColorBrush x:Key="NetCatWindowChromeBrush" Color="#253246" />
    <SolidColorBrush x:Key="NetCatSurfaceBrush" Color="#111A2B" />
    <SolidColorBrush x:Key="NetCatSurfaceAltBrush" Color="#162235" />
    <SolidColorBrush x:Key="NetCatSurfaceHeaderBrush" Color="#182538" />
    <SolidColorBrush x:Key="NetCatMutedTextBrush" Color="#8EA0B8" />
    <SolidColorBrush x:Key="NetCatStrongTextBrush" Color="#E8EEF7" />
    <SolidColorBrush x:Key="NetCatAccentBrush" Color="#4F8CFF" />
    <SolidColorBrush x:Key="NetCatAccentForegroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="NetCatAccentSoftBrush" Color="#264F8CFF" />
    <SolidColorBrush x:Key="NetCatScrollBarTrackBrush" Color="#142033" />
    <SolidColorBrush x:Key="NetCatScrollBarThumbBrush" Color="#4F8CFF" />
    <SolidColorBrush x:Key="NetCatScrollBarThumbBorderBrush" Color="#7AA6FF" />
    <SolidColorBrush x:Key="NetCatScrollBarThumbHoverBrush" Color="#6B9CFF" />
    <SolidColorBrush x:Key="NetCatScrollBarThumbDragBrush" Color="#88B0FF" />
    <SolidColorBrush x:Key="NetCatFooterBrush" Color="#0E1727" />

    <LinearGradientBrush x:Key="NetCatHeroGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Offset="0" Color="#0D1626" />
        <GradientStop Offset="1" Color="#111C2F" />
    </LinearGradientBrush>

    <Style x:Key="ModuleTitle" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize1}" />
    </Style>

    <Style x:Key="ToolbarTextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="StatusbarItem" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style TargetType="{x:Type TextElement}">
        <Setter Property="FontWeight" Value="Regular" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="FontWeight" Value="Regular" />
    </Style>

    <Style x:Key="lvItemSelected" TargetType="{x:Type ListViewItem}">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <materialDesign:Card Name="_Card" SnapsToDevicePixels="true">
                        <ContentPresenter />
                    </materialDesign:Card>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="_Card" Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary.Light}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GridViewColumnHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Height" Value="30" />
    </Style>

    <Style x:Key="ListItemCheckBox" BasedOn="{StaticResource MaterialDesignUserForegroundCheckBox}" TargetType="{x:Type CheckBox}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="DefButton" BasedOn="{StaticResource MaterialDesignRaisedButton}" TargetType="{x:Type ButtonBase}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="MinWidth" Value="96" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="TextBlock.LineHeight" Value="16" />
        <Setter Property="TextBlock.LineStackingStrategy" Value="BlockLineHeight" />
        <Setter Property="Foreground" Value="{DynamicResource NetCatAccentForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource NetCatAccentBrush}" />
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="14" />
    </Style>

    <Style x:Key="SubtleButton" BasedOn="{StaticResource MaterialDesignOutlinedButton}" TargetType="{x:Type ButtonBase}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="MinWidth" Value="88" />
        <Setter Property="Padding" Value="14,9" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="TextBlock.LineHeight" Value="16" />
        <Setter Property="TextBlock.LineStackingStrategy" Value="BlockLineHeight" />
        <Setter Property="Foreground" Value="{DynamicResource NetCatStrongTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource NetCatSurfaceAltBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NetCatWindowChromeBrush}" />
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="12" />
    </Style>

    <Style x:Key="DefContextMenu" BasedOn="{StaticResource MaterialDesignContextMenu}" TargetType="{x:Type ContextMenu}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="FontFamily" Value="{x:Static conv:MaterialDesignFonts.MyFont}" />
    </Style>

    <Style x:Key="ToolbarMenu" BasedOn="{StaticResource MaterialDesignMenu}" TargetType="{x:Type Menu}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="FontFamily" Value="{x:Static conv:MaterialDesignFonts.MyFont}" />
    </Style>

    <Style x:Key="DefComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}" TargetType="{x:Type ComboBox}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="Padding" Value="10,6" />
    </Style>

    <Style x:Key="DefDataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}" TargetType="{x:Type DataGrid}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="DefTextBox" BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="MyOutlinedTextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}" TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="Padding" Value="{StaticResource OutlinedTextBoxDefaultPadding}" />
        <Setter Property="MinHeight" Value="42" />
    </Style>

    <Style x:Key="MyOutlinedTextComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}" TargetType="{x:Type ComboBox}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
        <Setter Property="Padding" Value="{StaticResource OutlinedTextBoxDefaultPadding}" />
    </Style>

    <Style x:Key="MyChipListBoxItem" BasedOn="{StaticResource MaterialDesignChoiceChipPrimaryOutlineListBoxItem}" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Margin" Value="-2,0" />
    </Style>

    <Style x:Key="ListItemChip" TargetType="{x:Type materialDesign:Chip}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="ListItemTitle" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="ListItemSubTitle" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize}" />
    </Style>

    <Style x:Key="ListItemSubTitle2" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{DynamicResource StdFontSize-1}" />
    </Style>

    <Style x:Key="WindowGlobal" BasedOn="{StaticResource {x:Type Window}}" TargetType="{x:Type Window}">
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
        <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource NetCatStrongTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource NetCatWindowBackgroundBrush}" />
        <Setter Property="TextElement.FontFamily" Value="{x:Static conv:MaterialDesignFonts.MyFont}" />
        <Setter Property="FontFamily" Value="{x:Static conv:MaterialDesignFonts.MyFont}" />
        <Setter Property="ResizeMode" Value="CanResize" />
    </Style>

    <Style x:Key="ViewGlobal" BasedOn="{StaticResource {x:Type UserControl}}" TargetType="{x:Type UserControl}">
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
        <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource NetCatStrongTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource NetCatWindowBackgroundBrush}" />
        <Setter Property="TextElement.FontFamily" Value="{x:Static conv:MaterialDesignFonts.MyFont}" />
        <Setter Property="FontFamily" Value="{x:Static conv:MaterialDesignFonts.MyFont}" />
    </Style>

    <Style x:Key="ModernSlider" BasedOn="{StaticResource MaterialDesignSlider}" TargetType="{x:Type Slider}">
        <Setter Property="Foreground" Value="{DynamicResource NetCatAccentBrush}" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <Style x:Key="ScrollBarTrackButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RoundedScrollBarThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Background" Value="{DynamicResource NetCatScrollBarThumbBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NetCatScrollBarThumbBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        Margin="2"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource NetCatScrollBarThumbHoverBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource NetCatScrollBarThumbBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter Property="Background" Value="{DynamicResource NetCatScrollBarThumbDragBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource NetCatScrollBarThumbBorderBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RoundedVerticalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Border Background="{TemplateBinding Background}" CornerRadius="7" SnapsToDevicePixels="True">
            <Track x:Name="PART_Track" Margin="1" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource ScrollBarTrackButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb MinHeight="40" Style="{StaticResource RoundedScrollBarThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource ScrollBarTrackButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="RoundedHorizontalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Border Background="{TemplateBinding Background}" CornerRadius="7" SnapsToDevicePixels="True">
            <Track x:Name="PART_Track" Margin="1" IsDirectionReversed="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Style="{StaticResource ScrollBarTrackButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb MinWidth="40" Style="{StaticResource RoundedScrollBarThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Style="{StaticResource ScrollBarTrackButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Border>
    </ControlTemplate>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="{DynamicResource NetCatScrollBarTrackBrush}" />
        <Setter Property="Width" Value="14" />
        <Setter Property="Template" Value="{StaticResource RoundedVerticalScrollBarTemplate}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="14" />
                <Setter Property="Template" Value="{StaticResource RoundedHorizontalScrollBarTemplate}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="14" />
                <Setter Property="Height" Value="Auto" />
                <Setter Property="Template" Value="{StaticResource RoundedVerticalScrollBarTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CompactTabItem" TargetType="{x:Type TabItem}">
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="MinWidth" Value="148" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource NetCatMutedTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource NetCatSurfaceAltBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NetCatWindowChromeBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border
                        x:Name="TabBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="14">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource NetCatAccentSoftBrush}" />
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource NetCatAccentBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource NetCatStrongTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MyGroupBox" TargetType="{x:Type GroupBox}">
        <Setter Property="Margin" Value="0,0,0,14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Border
                        Background="{DynamicResource NetCatSurfaceBrush}"
                        BorderBrush="{DynamicResource NetCatWindowChromeBrush}"
                        BorderThickness="1"
                        CornerRadius="22"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border
                                Grid.Row="0"
                                Padding="18,14,18,12"
                                Background="{DynamicResource NetCatSurfaceHeaderBrush}"
                                CornerRadius="22,22,0,0">
                                <ContentPresenter ContentSource="Header">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="FontSize" Value="15" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="Foreground" Value="{DynamicResource NetCatStrongTextBrush}" />
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>
                            </Border>
                            <ContentPresenter Grid.Row="1" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
