<Window
    x:Class="v2rayN.Views.ConfirmDialogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    AllowsTransparency="True"
    Background="Transparent"
    Title="NetCat"
    MinWidth="360"
    MaxWidth="460"
    SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    ShowActivated="True"
    Style="{StaticResource WindowGlobal}"
    WindowStyle="None"
    WindowStartupLocation="CenterOwner">
    <Border
        Margin="10"
        Padding="24"
        Background="{DynamicResource NetCatSurfaceBrush}"
        BorderBrush="{DynamicResource NetCatWindowChromeBrush}"
        BorderThickness="1"
        CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon
                    Width="24"
                    Height="24"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource NetCatAccentBrush}"
                    Kind="AlertCircleOutline" />
                <TextBlock
                    x:Name="txtTitle"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="17"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource NetCatStrongTextBrush}"
                    Text="Confirm" />
            </StackPanel>

            <TextBlock
                x:Name="txtMessage"
                Grid.Row="1"
                Margin="0,16,0,0"
                FontSize="13"
                Foreground="{DynamicResource NetCatMutedTextBrush}"
                TextWrapping="Wrap" />

            <StackPanel
                Grid.Row="2"
                Margin="0,20,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    x:Name="btnCancel"
                    MinWidth="96"
                    Margin="0"
                    Content="Cancel"
                    IsCancel="True"
                    Style="{StaticResource SubtleButton}" />
                <Button
                    x:Name="btnConfirm"
                    MinWidth="100"
                    Margin="8,0,0,0"
                    Content="Confirm"
                    IsDefault="True"
                    Style="{StaticResource DefButton}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
