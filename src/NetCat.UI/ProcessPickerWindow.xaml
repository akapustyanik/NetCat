<Window x:Class="NetCat.UI.ProcessPickerWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:NetCat.UI" Title="Запущенные приложения" Width="760" Height="650" MinWidth="580" MinHeight="430" WindowStartupLocation="CenterOwner">
 <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel><Button Content="Обновить" Click="Refresh_Click" DockPanel.Dock="Right" Margin="12,18,0,10"/><StackPanel><TextBlock Text="Поиск по названию, пути или PID" Foreground="{DynamicResource MutedBrush}" FontSize="11"/><TextBox x:Name="Search" TextChanged="Search_Changed" ToolTip="Поиск по названию, пути или PID"/></StackPanel></DockPanel>
  <DockPanel Grid.Row="1" Margin="0,0,0,12"><CheckBox x:Name="BackgroundApps" Content="Показывать фоновые процессы" Click="Filter_Click"/><TextBlock x:Name="Count" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource MutedBrush}"/></DockPanel>
  <ListBox x:Name="Apps" Grid.Row="2" local:SmoothScroll.Enabled="True" VirtualizingPanel.ScrollUnit="Pixel" SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" MouseDoubleClick="Apps_DoubleClick">
   <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,0,0,6"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="Row" CornerRadius="9" BorderThickness="1" BorderBrush="Transparent" Padding="12" Background="{DynamicResource PanelBrush}"><ContentPresenter/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Row" Property="Background" Value="{DynamicResource RaisedBrush}"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="Row" Property="Background" Value="{DynamicResource AccentSoftBrush}"/><Setter TargetName="Row" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style></ListBox.ItemContainerStyle>
   <ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="48"/><ColumnDefinition/><ColumnDefinition Width="70"/></Grid.ColumnDefinitions><Image Source="{Binding Icon}" Width="32" Height="32" HorizontalAlignment="Left"/><StackPanel Grid.Column="1"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/><TextBlock Text="{Binding Path}" FontSize="11" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}"/></StackPanel><TextBlock Grid.Column="2" Text="{Binding PidLabel}" FontSize="11" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid></DataTemplate></ListBox.ItemTemplate>
  </ListBox>
  <DockPanel Grid.Row="3" Margin="0,16,0,0"><StackPanel Orientation="Horizontal" DockPanel.Dock="Right"><Button Content="Отмена" IsCancel="True"/><Button Content="Добавить выбранные" Style="{StaticResource PrimaryButton}" Click="Accept_Click" Margin="0"/></StackPanel><ComboBox x:Name="Route" Width="200" HorizontalAlignment="Left" Margin="0" DisplayMemberPath="Label" SelectedValuePath="Target"/></DockPanel>
 </Grid>
</Window>
